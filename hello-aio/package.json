{
  "name": "hello-aio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npx webpack --mode=production && npx mkdirp target && spin js2wasm -o target/hello-aio.wasm dist/spin.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mkdirp": "^3.0.1",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "@adobe/aio-sdk": "^5.0.1",
    "@fermyon/spin-sdk": "^0.6.0"
  }
}
